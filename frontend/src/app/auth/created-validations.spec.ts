import { CreatedValidations } from './created-validations';

describe('CreatedValidations', () => {
  it('should create an instance', () => {
    expect(new CreatedValidations()).toBeTruthy();
  });
});
